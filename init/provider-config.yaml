apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: aws-sqs
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: aws-secret
      key: creds
 